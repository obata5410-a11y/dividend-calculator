name: Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # 10分ごと
  workflow_dispatch:          # 手動実行も可能

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf https://dividend-calculator-6rsz.onrender.com/health || true
